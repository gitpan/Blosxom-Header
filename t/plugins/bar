package bar;
use strict;
use warnings;
use Blosxom::Header;

sub start { !$blosxom::static_entries }

sub last {
    my $header = Blosxom::Header->instance;
    $header->set( Bar => 'baz' );
    return;
}

1;
